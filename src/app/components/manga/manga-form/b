<div class="manga-form-container">
    <h2>Registro de Mangá</h2>
    <form [formGroup]="formGroup" (ngSubmit)="salvar()">
        <div class="form-group">
            <label for="nome">Nome:</label>
            <input type="text" id="nome" formControlName="nome" required>
            <div *ngIf="formGroup.get('nome')?.invalid && formGroup.get('nome')?.touched" class="error">
                {{ getErrorMessage('nome') }}
            </div>
        </div>
        <div class="form-group">
            <label for="paginas">Paginas:</label>
            <input type="text" id="paginas" formControlName="paginas" required>
            <div *ngIf="formGroup.get('paginas')?.invalid && formGroup.get('paginas')?.touched" class="error">
                {{ getErrorMessage('paginas') }}
            </div>
        </div>
        <div class="form-group">
            <label for="preco">Preço:</label>
            <input type="number" id="preco" formControlName="preco" required>
            <div *ngIf="formGroup.get('preco')?.invalid && formGroup.get('preco')?.touched" class="error">
                {{ getErrorMessage('preco') }}
            </div>
        </div>
        <div class="form-group">
            <label for="sinopse">Sinopse:<br></label>
            <textarea id="sinopse" formControlName="sinopse" required></textarea>
            <div *ngIf="formGroup.get('sinopse')?.invalid && formGroup.get('sinopse')?.touched" class="error">
                {{ getErrorMessage('sinopse') }}
            </div>
        </div>
        <div class="form-group">
            <label for="lancamento">Ano lançamento:</label>
            <input type="number" id="lancamento" formControlName="lancamento" required>
            <div *ngIf="formGroup.get('lancamento')?.invalid && formGroup.get('lancamento')?.touched" class="error">
                {{ getErrorMessage('lancamento') }}
            </div>
        </div>
        <div class="form-group">
            <label for="estoque">Estoque:</label>
            <input type="number" id="estoque" formControlName="estoque" required>
            <div *ngIf="formGroup.get('estoque')?.invalid && formGroup.get('estoque')?.touched" class="error">
                {{ getErrorMessage('estoque') }}
            </div>
        </div>
        <div class="form-group">
            <label for="color">Colorido:</label>
            <select id="color" formControlName="color" required>
                <option [value]="true">Sim</option>
                <option [value]="false">Não</option>
            </select>
        </div>
        <div class="form-group">
            <label for="idAutor">Autor:</label>
            <select id="idAutor" formControlName="idAutor" required>
                <option *ngFor="let autor of autores" [value]="autor.id">{{ autor.nome }}</option>
            </select>
            <div *ngIf="formGroup.get('idAutor')?.invalid && formGroup.get('idAutor')?.touched" class="error">
                {{ getErrorMessage('idAutor') }}
            </div>
        </div>
        <div class="form-group">
            <label for="genero">Gêneros:<br></label>
            <select id="genero" formControlName="genero" required>
                <option *ngFor="let genero of generos" [value]="genero[0]">{{ genero[1] }}</option>
            </select>
            <div *ngIf="formGroup.get('genero')?.invalid && formGroup.get('genero')?.touched" class="error">
                {{ getErrorMessage('genero') }}
            </div>
        </div>
        <div class="form-group">
            <label for="capitulos">Capítulos:</label>
            <input type="number" id="capitulos" formControlName="capitulos" required>
            <div *ngIf="formGroup.get('capitulos')?.invalid && formGroup.get('capitulos')?.touched" class="error">
                {{ getErrorMessage('capitulos') }}
            </div>
        </div>
        <button type="submit">Salvar</button>
        <button type="button" (click)="excluir()" *ngIf="formGroup.get('id')?.value">Excluir</button>
    </form>
</div>
